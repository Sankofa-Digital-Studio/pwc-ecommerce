name: Visual regression

on:
  workflow_dispatch:
  pull_request:
    branches:
      - dev
      - main
    paths:
      - ".storybook/**"
      - "src/**/*.stories.*"
      - "src/components/**"
      - "src/features/**"
      - "src/app/**"
      - "public/**"
      - "package.json"
      - "package-lock.json"

permissions:
  contents: read

jobs:
  chromatic:
    name: Chromatic visual regression
    if: ${{ vars.ENABLE_CHROMATIC == 'true' }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10
        with:
          fetch-depth: 0
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - name: Publish Storybook and compare baselines
        uses: chromaui/action@30047ab459164cf99ae8f097f6aa5c7ef4fcc869 # v13
        with:
          projectToken: ${{ secrets.CHROMATIC_PROJECT_TOKEN }}
          onlyChanged: true
